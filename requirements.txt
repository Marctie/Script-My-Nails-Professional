# Dipendenze leggere per app/live_server.py, pensate per girare su Termux
# (nessuna libreria di elaborazione immagini pesante: quelle sono in
# requirements-processing.txt e restano solo sul PC per backup.py/process.py).
Flask==3.1.3
flask-cors==6.0.5
python-dotenv==1.2.2
requests==2.34.2
WooCommerce==3.0.0
