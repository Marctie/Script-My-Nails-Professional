# Dipendenze leggere per app/live_server.py e app/build_static_site.py,
# pensate per girare su Termux (nessuna libreria di elaborazione immagini
# pesante: quelle sono in requirements-processing.txt e restano solo sul PC
# per backup.py/process.py). Pillow serve solo per ridimensionare le immagini
# in build_static_site.py, ma su Termux install.sh la filtra e usa al suo
# posto il pacchetto nativo "python-pillow" (niente wheel precompilata li').
Flask==3.1.3
python-dotenv==1.2.2
requests==2.34.2
WooCommerce==3.0.0
Pillow==12.3.0
